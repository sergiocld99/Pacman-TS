# Pacman en TypeScript

## Instalación
1. Compilar código mediante comando ````tsc```
2. Ejecutar LiveServer en carpeta dist, de esta forma el código JS generado puede interactuar con el documento HTML.